# Node 依赖（npm install 生成）
node_modules/

# 环境变量 / 密钥（真实配置不进库，模板 .env.example 需提交）
.env
.env.*
!.env.example

# npm 安装产物，各环境自行 install 生成
package-lock.json

# 运行日志与本地调试产物
*.log
npm-debug.log*
yarn-error.log*
