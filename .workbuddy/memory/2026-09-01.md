# 2026-09-01 工作日志

## 一键成片功能重构（手动剪辑 → AI 生成）
用户拍板：去手动剪辑、复用 ai_portrait 代理加路由、视频续写不上、不限额但保留文案、先 mock 假接口走通链路再接真实模型。

### 已完成
- **P1 代理**（`cloud_functions/ai_portrait/index.js`）：
  - 加 `POST /api/ai-video`（创建任务）+ `GET /api/ai-video/status?taskId=`（轮询状态）两路由
  - `MOCK_VIDEO=1` 模拟模式：0-10s PENDING → 10-20s RUNNING → 20s+ SUCCEEDED + `/mock/demo.mp4`（取自 `assets/seed/photos/feat_video_compressed.mp4` 拷贝）
  - 真实模式走百炼 `wan2.7-i2v-2026-04-25`，分辨率固定 720P，复用 `DASHSCOPE_API_KEY`/`MAAS_BASE_URL`
  - `express.json` limit 15mb → 40mb（20MB 图 base64 膨胀）
  - 验证脚本 `verify_mock_video.js` 已跑通全链路 ✅
- **P2 数据层**（`lib/data/ai_video_service.dart`）：AiVideoService 双路径（`AI_VIDEO_PROXY_URL` dart-define 走代理 / 否则演示模式加载内置 mp4）；`kAiVideoScenes` 6 个宠物场景选项卡（摇奶茶/摊煎饼/做咖啡/弹钢琴/踢足球/开小卖部）；`kAiVideoNegativePrompt` 宠物优化反向词；状态枚举映射万相
- **P3 UI**（`lib/app/ai_video_page.dart`）：compose/generating/result 三态；1 图位（相册/图库/拍照三来源）；场景选项卡 + 自定义 prompt；反向词折叠；时长 2-15s；水印开关；720P 固定；结果页预览/重生成/存短片库
- 入口：首页 feature 副标题改「一张照片让毛孩动起来」，route `/short-video` → `/ai-video`（app.dart + primary_tab_page.dart:110）
- `short_video_page.dart` 重写：删除 ShortVideoPage 手动剪辑，保留 ShortVideoLibraryPage/回放（短片库入口在 MinePage）

### 待办
- **构建验证未完成**：flutter build web 失败两次——① 第一次构建中途我改了 pages.dart 导致编译写入失败；② 重跑后 flutter 工具全部挂起，根因是 `D:\code\flutter\bin\cache\lockfile` + `flutter.bat.lock` 两个工具锁停留在构建失败时刻（15:10），已删锁，但 flutter 命令仍无输出，用户要求停止运行。**留给用户本地验证**：若 flutter 仍卡，PowerShell 删上述两锁后重跑 `flutter pub get && flutter build web --release`；mock 代理起服务 `PORT=3100 MOCK_VIDEO=1 node cloud_functions/ai_portrait/index.js`，前端连 `--dart-define=AI_VIDEO_PROXY_URL=http://localhost:3100`
- 接真实模型时：关 `MOCK_VIDEO` + `--dart-define=AI_VIDEO_PROXY_URL=https://<域名>`；注意真实模式 SUCCEEDED 返回 OSS 临时 URL，前端直接 fetch 下载（OSS 通常允许匿名 GET）
- 遗留：`pages.dart:513` 旧 slogan 文案含「一键成片」（无害）；照片 Picker 相册来源只读 photosProvider+capturedPhotosProvider，写真生成图未接入素材源
- mock 代理的 videoUrl 必须返回绝对 URL（`http://{host}/mock/demo.mp4`），前端部署域名 ≠ 代理域名时相对路径会 404（已修）

### 约定
- 万相 i2v endpoint：`{MAAS_BASE_URL}/services/aigc/video-generation/video-synthesis`（POST，X-DashScope-Async: enable），查询 `{MAAS_BASE_URL}/tasks/{task_id}`
- 视频输出比例跟随首帧图（微调至 16 倍数），分辨率 720P/1080P、时长 2-15s 按秒计费

## 二轮修改（16:24 用户反馈，已全部落地 + analyze 通过）
1. **必填项原处提示**：图片未选 → 图片区下方红字；prompt 空 → 输入框红框+红字；选择/输入即清除（`_imageError`/`_promptError`）
2. **时长默认 8s**（原 5s）
3. **保存到相册**：结果页按钮改「保存到相册」，存入新数据源 `lib/data/created_works.dart`（createdWorksProvider，图片/视频通用）
4. **相册「我的创作」分类**：`_AlbumModeTabs` 支持第三个 tab（`showCreatedTab` 可选，写真选图页不显示），`_AlbumView` 加 `_showCreated` 模式 + `_buildCreatedSlivers` 3 列网格（视频卡片+`_CreatedVideoDialog` 回放）；`pages.dart` 补 import created_works + media_platform
- 顺带修正过时文案：pages.dart 录制提示「去一键成片加字幕配乐」→「视频已存入短片库」
- 关键点：`_AlbumModeTabs` 有**两处**调用（AlbumPage 相册页 + `_PortraitAlbumPickerPage` 写真选图页），新参数需做成可选才不会破坏写真选图页
- flutter 工具锁问题已恢复（analyze 4.2s 跑通），之前删 lockfile/flutter.bat.lock 生效

## 三轮调整（16:32 用户反馈，截图指出层级）
- **「我的创作」降级为子筛选**：原放成顶层 tab 是错的，正确层级是「分类」tab 下的第一个 chip（替代原"全部" chip）
- 改回 `_AlbumModeTabs` 两 tab（删除第三个 tab 的 showCreatedTab/showCreated/onSelectCreated 参数）
- 相册页 `_petFilterBar` 第一个 tile 改用新建的 `_PetFilterCreatedTile`（label "我的创作", icon 同 classify3AiFill）；点 chip toggle `_showCreated` 并清空 `_selectedPetId`；点宠物 toggle `_selectedPetId` 并清 `_showCreated`
- 默认 `_showCreated=false && _selectedPetId=null` = 全部相册（无需"全部" chip）
- `_PortraitAlbumPickerPage` 写真选图页不动（仍用 `_PetFilterAllTile` "全部" chip）
- build slivers 分支：`if (_showCreated) ..._buildCreatedSlivers() else if (_byTime) ..._buildTimeSlivers() else ..._buildPetSlivers()`
- 验证：analyze 4 个原有 info，无 error/warning

## 四轮调整（16:46）
- **「全部」保留**：「我的创作」chip 排在「全部」之后，筛选带顺序 = [全部, 我的创作, 宠物1, 宠物2...]（itemCount = pets + 2）
- 状态互斥：全部 = `_selectedPetId==null && !_showCreated`；点我的创作清空宠物；点宠物清空我的创作
- analyze 通过

## 五轮调整（16:48）
- **结果页加下载按钮**：`MediaPlatform` 四平台新增 `downloadBytes(bytes, filename)`（Web=dart:html blob+`<a download>` 触发浏览器下载；IO=写系统临时目录；stub=UnsupportedError）
- 结果页底部栏从单按钮「保存到相册」改为 `_ResultActionsBar` 双按钮并排：左侧描边「下载」（MingCuteIcons.download）+ 右侧品牌黄「保存到相册」
- 下载文件名 `yuanbao_video_<毫秒时间戳>.mp4`，SnackBar 提示
- analyze 通过（4 个原有 info）

## 六轮：图标资产审计（18:08 自动化任务）
- 审计 `assets/icons/mingcute` 93 个 SVG 的真实引用情况（通过 `lib/app/mingcute_icons.dart` 常量定义 + lib 全源码交叉比对）
- 结论：**使用中 60 个；定义未用常量 9 个**（back-2/clapperboard/film/flower-2/heart/music/plus/right-small/settings-2）；**孤儿文件 24 个**（bone/brush/camera-2-off/cellphone/circle-dash/close/flash/flashlight/gift/happy/information/injection/left-small/moon/repeat/save/scale/settings-1/star/sticker/storage/sun/vip-1/volume-off）；冗余合计 33 个（35%）
- 额外发现：代码中贴纸/AI写真风格用 `LucideIcons.*`（另一套图标，brush/gift/heart/star/bone/flower2 等），MingCute 同名文件属重复资产
- 仅审计未删除；产物 `icon_audit_report.html` + `icon_audit.json` 在 `.workbuddy/automations/automation-1788167983303/`

## 2026-09-02 清理孤儿图标
- 用户确认"先删孤儿"，已删除 24 个零引用 SVG，目录 93 → 69 个，常量零缺失
- 剩余冗余：9 个定义未用常量（back-2/clapperboard/film/flower-2/heart/music/plus/right-small/settings-2）待清理
