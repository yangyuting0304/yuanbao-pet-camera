# 2026-08-28

## 毛孩写真接入真实 AI 模型 — 现状调研与模型分析

**项目**：yuanbao-pet-camera（元宝拍拍，Flutter Web + COS 静态托管 + ECS Node 代理 + 阿里云百炼/ModelScope）

**毛孩写真功能现状（PortraitPage）**
- 入口：首页功能卡 → `/portrait`（`lib/app/pages.dart:2396`）；UI 已完整：选照片（相册/拍摄）→ 6 风格（油画/水彩/动漫/复古胶片/国风/节日）→ 开始生成 → 结果页（`app_generated_result_page.dart`）→ 存相册
- 服务层 `lib/data/ai_portrait_service.dart`：三路径优先级 代理 URL > 直连 DashScope Key > 演示模式（回显源图）
- 代理契约：`POST {styleId, imageBase64} → {imageBase64}`，对应 `cloud_functions/ai_portrait/index.js`（POST /api/beautify，调千问 `wanx2.1-imageedit` 图生图 + COS 临时图床中转），契约已匹配
- **已知问题**：直连路径 `_submitTask` 用的是 `wanx2.1-t2i`（**文生图**，只发 prompt 不发源图），写真需要图生图，该路径名不符实且 Key 进前端不安全；真接通应只走 ECS 代理（`--dart-define=AI_PROXY_URL`）
- 待确认：代理是否已部署到 ECS（120.24.240.129，宝塔）、前端构建是否已注入 AI_PROXY_URL，决定线上是真 AI 还是演示模式

**模型分析结论**
- 需求本质：宠物照片→艺术风格化写真 = **图生图 + 主体(宠物)保留**；宠物特征保真比人像难
- 主方案：通义万相 wanx2.1-imageedit（代码 95% 就绪，免费 500 张/180天）
- 备选/质量升级：FireRed-Image-Edit（ModelScope，毛孩美颜已在用同架构，代理可复用）
- 差异化（后续）：一致性写真需支持 reference 保持的模型
- 部署路径：ECS 宝塔 + PM2 + 反向代理，与 firered-edit 同套路

**相关文件**：lib/app/pages.dart(2395-3050)、lib/data/ai_portrait_service.dart、lib/data/firered_service.dart、cloud_functions/{ai_portrait,firered_edit}/index.js、DEPLOY.md、README_setup.md

## 本地起 ai_portrait 代理联调（下午）
- 已创建 `cloud_functions/ai_portrait/.env`（填了 COS 桶 pet-camera-1322296918 / ap-guangzhou、PORT=3001，密钥留空待填）
- managed Node 22 装依赖（registry.npmmirror.com，141 packages），`node index.js` 后台跑在 **localhost:3001**
- 验证结果：`/healthz` → {"ok":true}；缺 imageBase64 → 400；带种子图 base64 → 走到 COS 上传报 `missing param SecretId`（证明链路已通到 COS，仅差凭据）
- **待用户提供**：DASHSCOPE_API_KEY（阿里云百炼）、TENCENT_COS_SECRET_ID、TENCENT_COS_SECRET_KEY；填好后测真实生成，再构建前端注入 `--dart-define=AI_PROXY_URL=http://localhost:3001/api/beautify`
- 注意：本地 curl 传大 base64 会 Argument list too long，需写临时 JSON 文件用 -d @file
- 本机 Flutter 3.44.9 可用（D:\code\flutter）

## 模型换新：wanx2.1-imageedit → wan2.7-image-pro（用户提供示例）
- 用户给到百炼新模型示例：工作空间专属域名 `https://ws-z867jv24od98hzwi.cn-beijing.maas.aliyuncs.com/api/v1`（WorkspaceId=z867jv24od98hzwi），接口 `POST /api/v1/services/aigc/multimodal-generation/generation`，请求 `input.messages[].content[{image},{text}]`，**同步返回**（无 async header），支持多图+风格化+特征一致
- 已重写 `cloud_functions/ai_portrait/index.js`：换 endpoint/请求体/容错响应解析（output.choices[].message.content[].image + 兼容老 results[].url），COS 上传/下载/清理逻辑保留，**前端契约 {styleId,imageBase64}→{imageBase64} 零改动**
- `.env`/`.env.example` 更新：MAAS_BASE_URL、MODEL=wan2.7-image-pro（可切 wan2.7-image）；本地服务已重启（localhost:3001），healthz ✓、链路验证到 COS（缺 SecretId）
- **仍待用户提供 3 密钥**：DASHSCOPE_API_KEY、TENCENT_COS_SECRET_ID、TENCENT_COS_SECRET_KEY
- 前端已构建注入 AI_PROXY_URL=http://localhost:3001/api/beautify，静态服务在 localhost:8080（python http.server），用户浏览器已打开可实测

## 本地真出图成功（用户给百炼 key + apiHost，去 COS 走 wan2.7）
- 用户决定走百炼 wan2.7，不走腾讯云；wan2.7 image 字段支持 base64 直传
- 第 1 次失败：`Failed to download image from [/9j/...]`（HTTP API 把裸 base64 当 URL 下载）
- 修复：代理里 `sniffMime()` 探测 jpeg/png/webp/bmp，包成 `data:image/jpeg;base64,...` data URI；重启服务
- 第 2 次成功：HTTP 200 / 16.2s / 返回 PNG 2K 结果；保存 test_result_oil.png（7.4MB），油画风格 9 张变体网格
- 观察：风格化（油画笔触/暖色调）✅；但 9 张猫表情姿态完全不同（非同一只猫），**特征保真不足**，需调 prompt（明确"保持图中这只猫的面部特征和长相"或上传参考图）
- 服务代码稳定，本地 8080 前端已可实测，点开始生成就能直接看到真 AI 结果

## 6 风格 prompt 优化版已同步生效
- 用户反馈油画感不强，6 个风格 prompt 全部重构：按「媒介技法→色彩光影→质感细节→背景氛围→保真约束」结构化，尾部统一"严格保持宠物原有的五官、毛色和姿态"
- 已同步 `cloud_functions/ai_portrait/index.js` 的 STYLE_PROMPTS（**实际生效**）+ `lib/data/ai_portrait_service.dart` 的 kPortraitStyles（直连兼容路径，保持一致）
- 代理已重启（localhost:3001，healthz ✓）；前端 dart 改动**无需重新构建**（代理路径只传 styleId，prompt 在服务端），浏览器刷新 8080 即可测试
- 关键认知：**前端 kPortraitStyles.prompt 在代理路径不生效，prompt 的真正源头是服务端 STYLE_PROMPTS**

## prompt 换用户英文专业版
- 用户提供 6 风格英文 prompt（油画 impasto/chiaroscuro、水彩 cold-pressed paper、动漫 cel-shaded、复古 Kodak Portra、国风 Gongbi、节日 cinematic），保真约束更强（no anthropomorphism / never morphology / non-negotiable）
- 已替换服务端 STYLE_PROMPTS（生效）+ 前端 kPortraitStyles（一致性），代理重启 healthz ✓，浏览器刷新 8080 即可测试
- 英文 prompt 用双引号包裹（内含撇号 subject's 等）
