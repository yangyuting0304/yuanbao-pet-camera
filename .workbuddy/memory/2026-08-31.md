# 2026-08-31

## 重启后端（ai_portrait 代理）
- 跨会话后旧进程仍在 3001（PID 22892），先 `MSYS_NO_PATHCONV=1 taskkill /F /PID 22892` 停掉（注意：Git Bash 下 taskkill 的 `/F` 会被路径转换吞掉，必须加 `MSYS_NO_PATHCONV=1`）
- 重新启动 `cloud_functions/ai_portrait/index.js`（managed Node 22），新 PID 24392
- 验证：healthz ✓；beautify 缺参 ✓；upload 报 `missing param SecretId`（COS 凭据仍未填，符合预期）
- 待办：① 提供 TENCENT_COS_SECRET_ID/KEY 才能让「保存到相册」的云持久化真正生效 ② 上次 flutter build 因 dart 残留进程锁文件失败（build/.dart_tool 已 rm），本地前端需重建才能在 8080 测试

## 前端重建完成（含坑记录）
- flutter build web 报错（写 build/web/assets/fonts/MaterialIcons-Regular.otf 和 .dart_tool/.../.filecache 失败）但**产物实际完整**：main.dart.js 3.4MB 今天生成、grep 确认 `localhost:3001` 已注入（2 处）
- 唯一残留：fonts/MaterialIcons-Regular.otf 是 8/28 旧文件未被覆盖（被占用），tree-shaken 内容相同，**不影响功能**
- 静态服务已起：python http.server 8080 --directory build/web；后端 localhost:3001 正常
- 经验：flutter build 报这类"写入失败"错误时先检查 build/web 产物是否完整 + grep main.dart.js 验证 dart-define，不必盲目重试
